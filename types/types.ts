

export interface TPosts {
    id: number,
    title: string,
    desc: string,
    category: string,
    author: string,
    cover: string
}

export type TCategory = {
    id: string,
    categoryName: string,
}